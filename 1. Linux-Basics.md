#### References: https://nostarch.com/linuxbasicsforhackers
## Why Learn Linux 2020
* Linux is open source:
_I mean the code, hence you can take a look and possibly modify, tinker and break it._

* Linux is the core of many modern technologies:
_Just to mention a few such as Android: Use heavily-modified Linux kernel, web server, super computer: The operating system (OS) run using Linux distribution, IoT devices such as IP camera, smart sensor and many more so-called smart devices have Linux kernel embeeded inside the firmware._

* Linux distribution popularity increasing:
_Linux distribution had many different flavour for desktop use such as Ubuntu, Linux Mint, Pop OS, Manjaro and many more. Hence, for some users it is a great alternative to Windows counterpart that is proprietary and closed-source. The adoption for Linux distribution also increasing in gaming space because there are thousands of game that natively support in Linux thanks to Steam, Proton and Lutris._

## The Linux File System
* `/root (The home directory of the all­ powerful root user)`
* `/etc (Generally contains the Linux configuration files that control when and how programs start up)`
* `/home (The user’s home directory)`
* `/mnt (Where other filesystems are attached or mounted to the filesystem)`
* `/media (Where CDs and USB devices are usually attached or mounted to the filesystem)`
* `/bin (Where application binaries the equivalent of executables in Microsoft Windows
reside)`
* `/lib (Where you’ll find libraries shared programs that are similar to Windows DLLs)`

## Basics Command in Linux
### Navigating the system
> pwd
* _Present working directory command, returns your location within the directory structure_

> whoami
* _To see which user you’re logged in as_

> cd /etc
* _Change to the /etc directory_

> cd ..
* _Move up one level in the file structure (toward the root of the file structure, or /)_
* _You would use ..  to move up one level._
* _You would use  .. ..  to move up two levels._
* _You would use  .. .. ..  to move up three levels, and so on._

> ls
* _See the contents of a directory (the files and subdirectories)_

> ls -l
* _Get more information about the files and directories, such as their permissions, owner, size and when they were last modified_

> ls -la
* _To also show hidden files_

> nmap -h 
> nmap --help 
> dnsrecon -? 
* _Short description of the tool and guidance on how to use it_

> man msfvenom
* _View a manual page_

### Finding stuff
> locate wordlists
* _Locate every occurrence of that word._

> whereis maltego
* _Returns not only the location of the binary but also its source and man page if they are available._

> which nc
* _Returns the location of the binaries in the PATH variable in Linux._

> find _directory options expression_ 
> find /➊ -type f➋ -name apache2➌ 
> find /etc -type f -name apache2 
> find /etc -type f --name apache2.* 
* _Search in any designated directory and looking for a number of different parameters_
* _Using wildcards, which enable us to match multiple characters._

> ps aux 
> ps aux | grep apache2 
* _See all the processes running on Linux system_
* _Find out whether the apache2 service is running using piping | and grep_

### Modifying files and directories
> cat > hackingskills
* _Create a file, follow the cat command with a redirect > symbol, and a name for the file we want to create._
* _Press ENTER , Linux will go into interactive mode and wait for you to start entering content for the file._

> cat hackingskills
* _Display the contents of that file._

> cat >> hackingskills
* _To add, or append, more content to a file, you can use the cat command with a double redirect >>_

> cat > hackingskills
* _Overwrite the file back with new information_

> touch newfile
* _Create a new empty file_

> mkdir newdirectory
* _Create a directory named newdirectory_

> cd newdirectory 
* _Navigate to this newly created directory_

> touch oldfile 
> cp oldfile  /root/newdirectory/newfile 
* _Create the file oldfile in the root directory with touch_
* _Copy it to /root/newdirectory_
* _Renaming it in the process and leaving the original oldfile in place_
* _Renaming the file is optional and is done simply by adding the name you want to give it to the end of the directory path._
